<Window
    x:Class="HRInPocket.WPF.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Width="350"
    Height="250"
    DataContext="{Binding LoginWindowVM, Source={StaticResource Locator}}"
    ResizeMode="NoResize"
    Topmost="True"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <StackPanel VerticalAlignment="Center">
        <TextBlock
            Margin="10,5"
            Foreground="Red"
            IsEnabled="False"
            Text="{Binding Message}"
            TextAlignment="Center" />
        <TextBlock
            Margin="10,5"
            Text="Имя пользователя:"
            TextAlignment="Center" />
        <TextBox Margin="50,5" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock
            Margin="10,5"
            Text="Пароль:"
            TextAlignment="Center" />
        <PasswordBox Name="txtPassword" Margin="50,5" />
        <Button
            Margin="100,10"
            Background="DeepSkyBlue"
            Command="{Binding LoginCommand}"
            CommandParameter="{Binding ElementName=txtPassword}"
            Content="Войти" />
        <Button
            Margin="100,5"
            Content="Отмена"
            IsCancel="True" />
    </StackPanel>
</Window>
