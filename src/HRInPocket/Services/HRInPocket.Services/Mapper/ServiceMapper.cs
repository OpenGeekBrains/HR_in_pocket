namespace HRInPocket.Services.Mapper
{
    // Нет сущности Service в БД

    public class ServiceMapper /*: BaseMapper<Service, ServiceDTO>, IMapper<Service, ServiceDTO>*/
    {
        //public ServiceDTO Mapping(Service entity) => ToDTO(entity,
        //    cfg => cfg.CreateMap<Service, ServiceDTO>());

        //public Service Mapping(ServiceDTO entity) => FromDTO(entity,
        //    cfg => cfg.CreateMap<ServiceDTO, Service>());
    }
}
